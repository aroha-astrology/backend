[project]
name = "aroha-swarm"
version = "0.1.0"
description = "Aroha Astrology — 6-agent Vedic orchestration swarm (FastAPI)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
