# --- Phase 0 core (runnable skeleton) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
pyswisseph>=2.10.3.2
firebase-admin>=6.5
orjson>=3.10
# Pure-Python fallback ephemeris for environments without a C toolchain
# (dev/CI on Windows where pyswisseph can't compile). Production uses pyswisseph.
skyfield>=1.49

# --- orchestration ---
langgraph>=0.2
langchain-core>=0.3

# --- LLM / streaming (Phase 1+) ---
openai>=1.40
langchain-openai>=0.2
sse-starlette>=2.1
httpx>=0.27
tenacity>=8.4
structlog>=24.1

# --- persistence / queue / cache (Phase 2.5+) ---
supabase>=2.6
redis>=5.0
arq>=0.26
alembic>=1.13
sqlalchemy>=2.0

# --- platform (launch hardening) ---
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.10
apscheduler>=3.10
python-telegram-bot>=21.4

# --- dev / test ---
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21
freezegun>=1.5
